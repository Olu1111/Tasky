# Environment Configuration
# Copy this file to .env and update with your actual values

# ===== Application Settings =====

# Node environment (development, production, test)
NODE_ENV=development

# Server port
PORT=4000

# ===== Database Configuration =====

# MongoDB connection URI
# REQUIRED: Connection string for MongoDB database
# Format: mongodb://[username:password@]host[:port]/database
# Or for MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/database
MONGODB_URI=mongodb://localhost:27017/tasky

# ===== Authentication & Security =====

# JWT secret key for signing tokens
# REQUIRED: Use a strong, random secret (minimum 32 characters recommended)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# JWT token expiration time
# Optional: Defaults to 7d (7 days)
# Examples: 60, "2 days", "10h", "7d"
JWT_EXPIRES_IN=7d

# ===== CORS Configuration =====

# Allowed origin for CORS requests
# Optional: Defaults to http://localhost:5173
# For production, set to your frontend domain
# Examples: https://yourdomain.com, https://app.example.com
CORS_ORIGIN=http://localhost:5173

# ===== Rate Limiting =====
# Rate limiting is configured in code with the following defaults:
# - Global rate limit: 300 requests per 15 minutes
# - Auth login: 5 attempts per 15 minutes
# - Registration: 3 attempts per hour

